<!DOCTYPE html>
<html lang="en">
 <head>
  
  <title>Document</title>
 </head>
 <body>
    <script>
	function emp(id,name,salary){
		this.id=id;
		this.name=name;
		this.salary=salary;
		this.changeSalary=changeSalary;
        function changeSalary(otherSalary){
		this.salary=otherSalary
	
        }
		}

		e=new emp(101,"giri",3000);
		document.write(e.id+" "+e.name+" "+e.salry);
		e.changeSalary(4000);
		document.write("<br>"+e.id+" "+e.name+" "+e.salary);
	</script>
  
 </body>
</html>
